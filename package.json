{
  "name": "@t117503445/s3-database",
  "version": "2.1.1",
  "description": "JavaScript SDK uses S3 as the data source, to build applications that don't need a backend.",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./config/tsconfig.build.json",
    "prebuild": "tsc -p ./config/tsconfig.prebuild.json",
    "test": "ts-mocha -p ./config/tsconfig.test.json ./test/*",
    "doc": "typedoc --tsconfig ./config/tsconfig.doc.json  --options ./config/typedoc.json"
  },
  "keywords": [
    "S3",
    "CRUD",
    "REST"
  ],
  "author": "117503445",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^9.0.0",
    "@types/node-fetch": "2",
    "mocha": "^9.1.3",
    "ts-mocha": "^8.0.0",
    "typedoc": "^0.22.7",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.37.0",
    "axios": "^0.23.0",
    "node-fetch": "2"
  }
}
